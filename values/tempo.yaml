tempo:
  storage:
    trace:
      backend: local
      local:
        path: /var/tempo/traces

config: |
  server:
    http_listen_port: 3200

  distributor:
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

  storage:
    trace:
      backend: local
      local:
        path: /var/tempo/traces





#tempo:
#  # simple single-binary deployment suitable for many clusters; scale as needed
#  persistence:
#    enabled: true
#    size: 20Gi
#    storageClassName: "{{ .Values.cluster.storageClassName }}"####
#
#  service:
#    type: ClusterIP##
#
#  # Enable OTLP receivers
#  config:
#    receivers:
#      otlp:
#        protocols:
#          grpc:
#          http:
